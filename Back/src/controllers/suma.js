export const sumaController = async (req, res) => {
  const { num1, num2 } = req.body;

  try {
    const nuevaSuma = async;
  } catch (error) {}
  const result = num1 + num2;
  res.json({ result });
};
